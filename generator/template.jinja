<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="/static/styles/colors.css" />
    <link rel="stylesheet" href="/static/styles/global.css" />
    <link rel="stylesheet" href="/static/styles/code.css" />

    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href="/static/icon/apple-touch-icon.png"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="32x32"
      href="/static/icon/favicon-32x32.png"
    />
    <link
      rel="icon"
      type="image/png"
      sizes="16x16"
      href="/static/icon/favicon-16x16.png"
    />
    <link rel="manifest" href="/static/icon/site.webmanifest" />
    <link rel="mask-icon" href="/static/icon/safari-pinned-tab.svg" color="#bd93f9" />
    <link rel="shortcut icon" href="/static/icon/favicon.ico" />
    <meta name="msapplication-TileColor" content="#603cba" />
    <meta name="msapplication-config" content="/static/icon/browserconfig.xml" />
    <meta name="theme-color" content="#282A36" />


    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta charset="UTF-8" />
    <title>notes: {{title.lower()}}</title>
  </head>

  <body>
     <nav>
      <div>
        <a href="/">
          <img src="/static/icon/icon.png" alt="notes" height="100">
        </a>
      </div>
      

      {% if piblings != None %}
      <div>
        {% if len(piblings) > 9 %}
        <details><summary>{{len(piblings)}} piblings</summary>
        {% endif %}
        <ul>
        {% for pibling in piblings %}
          <li>
            {% if pibling == parent %}
            <a href="/{{sanitize_url(pibling)|e}}" class="activated-link">{{pibling}}</a>
            {% else %}
            <a href="/{{sanitize_url(pibling)|e}}">{{pibling}}</a>
            {%endif%}
          </li>
        {% endfor %}
        </ul>
        {% if len(piblings) > 9 %}
        </details>
        {% endif %}
      </div>
      {% endif %}

      {% if siblings != None %}
      <div>
        {% if len(siblings) > 9 %}
        <details><summary>{{len(siblings)}} siblings</summary>
        {% endif %}
        <ul>
        {% for sibling in siblings %}
          <li>
            {% if sibling == title %}
            <a href="/{{sanitize_url(sibling)|e}}" class="activated-link">{{sibling}}</a>
            {% else %}
            <a href="/{{sanitize_url(sibling)|e}}">{{sibling}}</a>
            {%endif%}
          </li>
        {% endfor %}
        </ul>
        {% if len(siblings) > 9 %}
        </details>
        {% endif %}
      </div>
      {% endif %}



      {% if children != None and len(children) > 0 %}
      <div>
        {% if len(children) > 9 %}
        <details><summary>{{len(children)}} children</summary>
        {% endif %}
        <ul>
        {% for child in children %}
          <li>
            <a href="/{{sanitize_url(child)|e}}">{{child}}</a>
          </li>
        {% endfor %}
        </ul>
        {% if len(children) > 9 %}
        </details>
        {% endif %}
      </div>
      {% endif %}

    </nav>
    <main>
      <header>
        <h1>{{title}}</h1>
      </header>
      <article>{{body}}</article>

      {% if len(backlinks) > 0 %}
      <p class="backlinks"><span class="bold">Incoming:</span>
          {% for backlink in backlinks %}
          <a href="/{{sanitize_url(backlink)|e}}">{{backlink}}</a>
          {% endfor %}
        </span>
      </p>
      {% endif %}
        


      {% if children != None%}
        {% for child in children %}
        <h2><a href="/{{sanitize_url(child)|e}}">{{child}}</a></h2>
        {% endfor %}
      {% endif %}

    </main>
  </body>
</html>
